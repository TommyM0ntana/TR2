<%= provide(:button_text, 'Unfollow')%>
  <%= bootstrap_form_with(model: current_user.active_relationships.find_by(followed_id: @user.id),html: { method: :delete }, local: true) do |f| %>
    <%= f.submit yield(:button_text), class: 'btn btn-lg btn-info' %>
  <% end %>